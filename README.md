# Discord GIF Reaction Bot

Бот для Discord, написанный на Python с использованием библиотеки `discord.py`. Автоматически реагирует на определённые гифки (по URL или провайдеру Tenor) и отправляет случайные картинки из локальной папки `images`. Также предоставляет команду `!image` для ручной отправки случайной картинки.

## 🚀 Функции

- **Автоматическая реакция на гифки**: Если в сообщении содержится целевой URL гифки (настраивается через переменную окружения `TARGET_GIF_URL`) или embed от провайдера Tenor, бот отправляет случайную картинку из папки `images`.
- **Ручная отправка картинок**: Команда `!image` отправляет случайную картинку из локальной папки.
- **Логирование embed'ов**: В консоли выводится детальная информация о каждом embed'е в сообщениях (для отладки и настройки).
- **Поддержка локальных изображений**: Все картинки хранятся локально в папке `images` (форматы: .jpg, .png, .gif и др.).

## 🐳 Запуск через Docker

Вы можете запустить бота в Docker-контейнере, установив [Docker Desktop](https://www.docker.com/)

### Docker

1. **Запуск**
   ```bash
   git clone https://github.com/CEPIbN/Discord-bot.git
   ```
   ```bash
   cd Discord-bot
   ```
   ```bash
   docker-compose up -d
   ```

2. **Остановка**

   ```bash
   docker-compose down
   ```

Файл `docker-compose.yml` автоматически:
- Собирает образ из `Dockerfile`.
- Загружает переменные из `.env`.
- Монтирует папки `images` и `logs`.
- Настраивает политику перезапуска (`restart: unless-stopped`).
- Ограничивает размер логов.

### Примечания

- Убедитесь, что файл `.env` существует и содержит корректный `BOT_TOKEN`.
- Папка `images` должна существовать на хосте (или будет создана при первом запуске `docker-compose`).
- Если вы добавляете новые картинки, перезапускать контейнер не требуется – они подхватятся автоматически благодаря монтированию тома.
- Для обновления зависимостей (изменения в `requirements.txt`) пересоберите образ: `docker-compose build --no-cache` или `docker build --no-cache`.

## 📦 Установка и настройка вручную

### 1. Клонирование репозитория
```bash
git clone https://github.com/CEPIbN/Discord-bot.git
```
```bash
cd Discord-bot
```

### 2. Установка зависимостей
Убедитесь, что у вас установлен Python 3.13 или выше. Затем установите зависимости:
```bash
pip install -r requirements.txt
```

### 3. Установка FFmpeg
Для работы музыкальных команд (воспроизведение аудио) требуется FFmpeg. Установите его в зависимости от вашей операционной системы:

#### Windows
1. **Ручная установка**: Скачайте статическую сборку с [официального сайта FFmpeg](https://ffmpeg.org/download.html) или через [gyan.dev](https://www.gyan.dev/ffmpeg/builds/). Распакуйте архив и добавьте путь к папке `bin` в переменную окружения `PATH`.
2. **Chocolatey** (если установлен):
   ```bash
   choco install ffmpeg
   ```
3. **Scoop** (если установлен):
   ```bash
   scoop install ffmpeg
   ```
4. Проверьте установку, выполнив в командной строке:
   ```bash
   ffmpeg -version
   ```

#### Linux (Debian/Ubuntu)
```bash
sudo apt update
sudo apt install ffmpeg
```

#### macOS (с использованием Homebrew)
```bash
brew install ffmpeg
```

После установки убедитесь, что команда `ffmpeg` доступна в терминале.

### 4. Настройка переменных окружения
Создайте файл `.env` в корне проекта и добавьте следующие переменные:
```env
BOT_TOKEN=your_discord_bot_token_here
TARGET_GIF_URL=https://example.com/target.gif  # опционально, URL гифки-триггера
SKIP_GIF=https://example.com/skip.gif          # опционально, URL гифки для реакции на команду !skip
```
- **BOT_TOKEN**: Токен вашего Discord-бота. Получить можно на [Discord Developer Portal](https://discord.com/developers/applications).
- **TARGET_GIF_URL**: (Опционально) URL гифки, на которую бот будет реагировать. Если не задан, бот будет реагировать только на embed'ы от Tenor и гифки с ID `25572384`.
- **SKIP_GIF**: (Опционально) URL гифки, которая будет отправлена в ответ на команду `!skip`, если бот не находится в голосовом канале или не воспроизводит музыку. Если не задана, бот отправит текстовое сообщение.

### 5. Подготовка папки с картинками
Создайте папку `images` в корне проекта и поместите туда любые изображения (поддерживаемые Discord форматы: JPG, PNG, GIF и т.д.):
```bash
mkdir images
```
Бот автоматически загрузит все файлы из этой папки при запуске.

## 🎮 Использование

### Запуск бота
```bash
python bot.py
```
При успешном запуске в консоли появится сообщение:
```
Бот <имя_бота> успешно запущен!
Загружено <N> локальных картинок.
```

### Команды
- `!image` – отправляет случайную картинку из папки `images` в текущий канал.

#### Музыкальные команды
- `!play <запрос или ссылка>` – воспроизводит аудио из YouTube (или другого поддерживаемого источника) в голосовом канале. Бот присоединится к вашему каналу и начнёт воспроизведение.
- `!stop` – останавливает воспроизведение, отключает бота от голосового канала и очищает очередь.
- `!skip` – пропускает текущий трек и переходит к следующему в очереди. Если очередь пуста, бот отключается через 5 минут бездействия.

### Автоматические реакции
1. **Реакция на текст**: Если сообщение содержит URL, указанный в `TARGET_GIF_URL`, бот отправит случайную картинку.
2. **Реакция на embed'ы**: Бот анализирует все embed'ы в сообщении. Если обнаружен embed с URL, содержащим `25572384`, или провайдером `Tenor`, бот также отправит случайную картинку.

### Отладка
В консоли выводятся подробные данные о каждом embed'е (тип, URL, заголовок, описание и т.д.). Это помогает настроить триггеры под нужные гифки.

## ⚙️ Конфигурация

Настройки бота задаются в коде `bot.py`:

| Переменная | Описание | По умолчанию |
|------------|----------|--------------|
| `TARGET_GIF_URL` | URL гифки-триггера (из переменной окружения) | `None` |
| `IMAGES_FOLDER` | Папка с локальными картинками | `"images"` |
| `BOT_TOKEN` | Токен бота (из переменной окружения) | – |
| `COMMAND_PREFIX` | Префикс команд | `!` |

Вы можете изменить эти параметры, отредактировав `bot.py`.

## 📋 Зависимости

Проект использует следующие Python-пакеты (см. `requirements.txt`):
- `discord.py==2.7.1` – библиотека для взаимодействия с Discord API.
- `python-dotenv==1.2.2` – загрузка переменных окружения из `.env` файла.
- `yt-dlp==2026.3.3` – инструмент для загрузки и извлечения аудио с YouTube и других платформ (используется для музыкальных команд).
- `PyNaCl==1.6.2` – библиотека для шифрования, необходима для работы голосовых функций Discord.
- `davey==0.1.4` – вспомогательная библиотека для работы с аудио (используется внутренне `discord.py`).

## 🗂️ Структура проекта
```
Discord-bot/
├── bot.py                 # Основной код бота
├── requirements.txt       # Зависимости Python
├── .env                  # Переменные окружения (не в репозитории)
├── images/               # Папка с картинками (создаётся вручную)
└── README.md             # Документация
```

## 🔧 Возможные проблемы и решения

- **Бот не запускается**: Убедитесь, что токен бота указан верно и у бота есть необходимые права (например, `Send Messages`, `Read Message Content`).
- **Картинки не отправляются**: Проверьте, что папка `images` существует и содержит файлы. Также убедитесь, что бот имеет право отправлять файлы в канал.
- **Бот не реагирует на гифки**: Проверьте, что переменная `TARGET_GIF_URL` задана корректно, либо что embed содержит нужный URL или провайдера. Используйте вывод отладки в консоли для анализа embed'ов.

## P.S.

Проект создан по приколу :)